<UserControl x:Class="NanoAtm.Views.Controls.DispenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NanoAtm.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="5" Padding="15">
        <StackPanel>
            <TextBlock Text="Пожалуйста, заберите деньги" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <local:CashBundleView DataContext="{Binding Bundle}" Margin="10"/>

            <Button Command="{Binding TakeCommand}" IsDefault="True" HorizontalAlignment="Center" Margin="10" Background="#FF72C472">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Забрать" FontSize="24" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Countdown, StringFormat=({0})}" FontSize="24"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</UserControl>