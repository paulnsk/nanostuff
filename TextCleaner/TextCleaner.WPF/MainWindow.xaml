<Window x:Class="TextCleaner.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextCleaner.WPF"
        xmlns:wpfStuff="clr-namespace:TextCleaner.WPF.WpfStuff"
        mc:Ignorable="d"
        Title="Text cleaner" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Добавить файлы" Command="{Binding AddFilesCommand}" HorizontalAlignment="Left" Padding="10,5"/>

        <StatusBar Grid.Row="1" Margin="0,5,0,5">
            <StatusBarItem>
                <TextBlock Text="{Binding CurrentProgress}"/>
            </StatusBarItem>
        </StatusBar>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Очередь" Margin="0,0,5,0">
                <ListView ItemsSource="{Binding QueuedFiles}"/>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Лог" Margin="5,0,0,0">
                <TextBox wpfStuff:TextBoxExtensions.BoundItemsSource="{Binding LogMessages}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
